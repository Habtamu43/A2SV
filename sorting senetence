class Solution(object):
    def sortSentence(self, s):
        output=""
        array=s.split()
        for i in range(1,10):
            for word in array:
                if word[-1]==str(i):
                    output+=" " + word[:-1]
        return output.strip()

