class Solution(object):
    def targetIndices(self, nums, target):
        nums=sorted(nums)
        output_array=[]
        for i in range(len(nums)):
            if target==nums[i]:
                output_array.append(i)
        return output_array
                 
                 
